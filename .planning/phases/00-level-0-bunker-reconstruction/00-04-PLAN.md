---
phase: 00-level-0-bunker-reconstruction
plan: 04
type: execute
wave: 4
depends_on:
  - 00-03
files_modified:
  - src/hooks/useJointFitnessPlan.ts
  - src/native/screens/Level0BunkerReconstruction.tsx
  - src/lib/supabase.ts
autonomous: true
requirements:
  - P0-JOINT-OPS-FITNESS
  - P0-GHOST-OPS
  - P0-THREE-QUESTION-MAX
  - P0-ADAPTIVE-ACTIONS
must_haves:
  truths:
    - Fitness is presented as a shared Bunker mission, not a hardcoded workout selector.
    - The system asks no more than three high-yield questions before drafting a first action.
    - Skip, substitute, shuffle, defer, and correction all update the behavior event stream.
    - Ghost Ops lets one user participate quietly without blocking Joint Ops.
  artifacts:
    - Joint fitness planning hook
    - Bunker screen integration
    - DB type additions if plan 02 did not already cover required fields
  key_links:
    - Plan actions connect to Bunker state changes and behavior_events.
---

<objective>
Wire the personal-priority fitness path into Level 0 without rebuilding legacy hardcoded workouts.

Purpose: Let Cade and his wife test whether shared fitness momentum can be made playable immediately.
Output: A Joint Ops/Ghost Ops fitness flow inside the Bunker vertical slice.
</objective>

<execution_context>
@C:/Users/Windows 11 Pro/Desktop/Spectre_Labs/Feu_Follet/Tether_Safe/.Codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Windows 11 Pro/Desktop/Spectre_Labs/Feu_Follet/Tether_Safe/.Codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/FITNESS_PLAN.md
@.planning/TETHER_ML_ARCHITECTURE.md
@.planning/LEVEL_0_BUNKER_RECONSTRUCTION.md
@src/hooks/useJointOps.ts
@src/native/screens/Level0BunkerReconstruction.tsx
@src/lib/supabase.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Joint Fitness plan hook</name>
  <files>src/hooks/useJointFitnessPlan.ts</files>
  <behavior>
    - The hook asks at most three questions.
    - It creates a draft action from answers and existing signals.
    - It exposes complete, skip, substitute, shuffle, defer, and correction handlers.
    - It does not use fixed workout programs as source of truth.
  </behavior>
  <action>Create a typed hook for Phase 0 joint fitness planning. It may use deterministic local drafting until an AI provider is selected, but the contract must model AI-generated/adaptive output and preserve user corrections. Follow DB-first writes and explicit return type conventions.</action>
  <verify>
    <automated>npx tsc --project tsconfig.app.json --noEmit</automated>
  </verify>
  <done>Hook can drive a first adaptive movement action and log every user response as behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Joint Ops and Ghost Ops into Bunker screen</name>
  <files>src/native/screens/Level0BunkerReconstruction.tsx</files>
  <action>Add the fitness mission panel to the Bunker screen. Use Joint Ops language for shared mode and Ghost Ops for quiet participation. Keep controls practical and compact. Do not reintroduce the old domain grid or fixed Push Day path.</action>
  <verify>
    <automated>npx tsc --project tsconfig.app.json --noEmit</automated>
  </verify>
  <done>User can start from the Bunker, answer no more than three questions, receive one action, and honestly change or complete it.</done>
</task>

<task type="auto">
  <name>Task 3: Verify behavior coverage</name>
  <files>src/hooks/useJointFitnessPlan.ts, src/native/screens/Level0BunkerReconstruction.tsx</files>
  <action>Audit the final flow for every required behavior action: complete, skip, substitute, shuffle, defer, correction, and partner response. Add missing handlers or labels only where needed.</action>
  <verify>
    <automated>powershell -NoProfile -Command "Select-String -Path 'src/hooks/useJointFitnessPlan.ts','src/native/screens/Level0BunkerReconstruction.tsx' -Pattern 'complete','skip','substitute','shuffle','defer','correction','Ghost Ops' -Quiet"</automated>
  </verify>
  <done>Every required action is reachable from code and writes or queues a behavior event.</done>
</task>

</tasks>

<verification>
Run `npx tsc --project tsconfig.app.json --noEmit`, `npm run lint`, and native manual verification through `npx expo start` on the selected target.
</verification>

<success_criteria>
Phase 0 supports a real two-person fitness accountability slice while preserving the product law that no hardcoded workout plan is the foundation.
</success_criteria>

<output>
After completion, create `.planning/phases/00-level-0-bunker-reconstruction/00-04-SUMMARY.md`.
</output>
