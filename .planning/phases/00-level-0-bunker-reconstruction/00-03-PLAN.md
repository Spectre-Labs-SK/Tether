---
phase: 00-level-0-bunker-reconstruction
plan: 03
type: execute
wave: 3
depends_on:
  - 00-02
files_modified:
  - src/native/screens/Level0BunkerReconstruction.tsx
  - src/native/NativeApp.tsx
  - src/native/navigation.types.ts
  - src/hooks/useLevel0Bunker.ts
autonomous: true
requirements:
  - P0-BUNKER-VERTICAL-SLICE
  - P0-VISUAL-MODES
  - P0-CHAOS-EVENT
  - P0-INTEL-DROP
must_haves:
  truths:
    - The first native screen communicates "the house is under attack; rebuild the base by doing real things" in under 30 seconds.
    - Military, Ethereal, and Mixed modes change the Level 0 mood.
    - Task completion physically improves the base; degradation is silent and non-shaming.
    - A locked door, Intel Drop, and one chaos event are visible in the slice.
  artifacts:
    - Level0BunkerReconstruction native screen
    - Bunker state hook
    - Native navigation route
  key_links:
    - Bunker actions write behavior_events from plan 02.
---

<objective>
Build the first playable native Level 0 Bunker Reconstruction loop.

Purpose: Prove the fun loop before expanding modules.
Output: A native vertical slice where real actions repair a damaged base while household chaos attacks it.
</objective>

<execution_context>
@C:/Users/Windows 11 Pro/Desktop/Spectre_Labs/Feu_Follet/Tether_Safe/.Codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Windows 11 Pro/Desktop/Spectre_Labs/Feu_Follet/Tether_Safe/.Codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/LEVEL_0_BUNKER_RECONSTRUCTION.md
@.planning/BUILD_PLAN.md
@.planning/FITNESS_PLAN.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@src/native/NativeApp.tsx
@src/native/navigation.types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Level 0 state hook</name>
  <files>src/hooks/useLevel0Bunker.ts</files>
  <action>Create a hook that owns Level 0 state: visual mode, degradation level, repaired sections, locked door progress, Intel Drop earned/opened state, and active chaos event. Use local state for screen state and write behavior_events to Supabase first for user actions. Keep return type explicit. Do not use Zustand unless a render-loop consumer is introduced.</action>
  <verify>
    <automated>npx tsc --project tsconfig.app.json --noEmit</automated>
  </verify>
  <done>Hook exposes typed actions for complete, skip, substitute, shuffle, defer, correction, and chaos response.</done>
</task>

<task type="auto">
  <name>Task 2: Build native Bunker screen</name>
  <files>src/native/screens/Level0BunkerReconstruction.tsx</files>
  <action>Create the native screen using StyleSheet.create and file-top COLORS constants. Include Military, Ethereal, and Mixed modes; silent degradation; a damaged base area; one visible locked door; one Intel Drop labeled `// INTEL RECOVERED`; one household chaos/base-attack event; and action controls for complete, skip, substitute, shuffle, defer, and correction. Do not create a marketing/landing screen.</action>
  <verify>
    <automated>npx tsc --project tsconfig.app.json --noEmit</automated>
  </verify>
  <done>The screen is usable as the first app surface and makes the Bunker loop understandable without explanatory feature text.</done>
</task>

<task type="auto">
  <name>Task 3: Verify Android target path</name>
  <files>app.json, eas.json</files>
  <action>Use Android as the Phase 0 native verification target per D-00-07. Confirm Expo/EAS Android configuration is present and do not add iOS-specific work in this plan. If a native manual run is needed, use the Android path first.</action>
  <verify>
    <automated>powershell -NoProfile -Command "Select-String -Path 'app.json' -Pattern 'android','com.spectrelabs.tether' -Quiet"</automated>
  </verify>
  <done>Phase 0 native verification proceeds Android-first without another target decision checkpoint.</done>
</task>

</tasks>

<verification>
Run `npx expo start` and verify the Android target first. Capture screenshots of Military, Ethereal, and Mixed modes for human review.
</verification>

<success_criteria>
The first native screen proves the Level 0 joke and loop: chaos attacks the base, one real action improves it, and the system records the behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/00-level-0-bunker-reconstruction/00-03-SUMMARY.md`.
</output>
