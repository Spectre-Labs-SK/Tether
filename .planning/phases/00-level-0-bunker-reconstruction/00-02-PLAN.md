---
phase: 00-level-0-bunker-reconstruction
plan: 02
type: execute
wave: 2
depends_on:
  - 00-01
files_modified:
  - .planning/data-model.md
  - supabase/migrations/07_behavior_events_and_questions.sql
  - supabase/migrations/08_generated_plans_and_screenshots.sql
  - src/lib/supabase.ts
autonomous: true
requirements:
  - P0-BEHAVIOR-EVENTS
  - P0-WIPE-DATA
  - P0-KILL-SWITCH-STUB
  - P0-SCREENSHOT-INGESTION
must_haves:
  truths:
    - Completion, skip, substitute, shuffle, defer, correction, and chaos response can be persisted.
    - Screenshot ingestion stores images and metadata without bank access or parsing requirements.
    - Wipe data and kill switch contracts exist before broad feature work.
  artifacts:
    - .planning/data-model.md
    - supabase/migrations/07_behavior_events_and_questions.sql
    - supabase/migrations/08_generated_plans_and_screenshots.sql
    - Updated DB types in src/lib/supabase.ts
  key_links:
    - UI actions can write through typed Supabase contracts.
---

<objective>
Define and implement the Phase 0 data model contracts before UI work expands.

Purpose: Make behavior tracking real from first interaction and create trust-critical data controls early.
Output: Data-model doc, migrations, and TypeScript DB types.
</objective>

<execution_context>
@C:/Users/Windows 11 Pro/Desktop/Spectre_Labs/Feu_Follet/Tether_Safe/.Codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Windows 11 Pro/Desktop/Spectre_Labs/Feu_Follet/Tether_Safe/.Codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/FITNESS_PLAN.md
@.planning/TETHER_ML_ARCHITECTURE.md
@.planning/INTELLIGENCE_LAYER.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@src/lib/supabase.ts
@supabase/migrations/
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the Phase 0 data model contract</name>
  <files>.planning/data-model.md</files>
  <behavior>
    - The doc names behavior_events, question_sessions, generated_plans, plan_steps, plan_actions, screenshot_ingestions, accounts, transactions, envelopes, pantry_items, pendulum_events, and noseyquestions_log.
    - The doc marks direct bank access and automated money movement out of scope.
    - The doc states wipe data is full deletion, not archive.
  </behavior>
  <action>Create the data-model doc from the Phase 0 source specs. Keep it implementation-oriented: table purpose, required relationships, key enums, and trust boundaries. Do not add broad future module detail beyond the fields needed to keep Phase 0 migrations coherent.</action>
  <verify>
    <automated>powershell -NoProfile -Command "Select-String -Path '.planning/data-model.md' -Pattern 'behavior_events','question_sessions','generated_plans','screenshot_ingestions','wipe data','No direct bank access' -Quiet"</automated>
  </verify>
  <done>Data model doc can be used directly to write migrations without asking product-scope questions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Phase 0 migrations</name>
  <files>supabase/migrations/07_behavior_events_and_questions.sql, supabase/migrations/08_generated_plans_and_screenshots.sql</files>
  <behavior>
    - behavior_events can represent complete, skip, substitute, shuffle, defer, correction, and chaos response.
    - question_sessions stores up to three questions per plan attempt.
    - generated_plans, plan_steps, and plan_actions preserve what Tether suggested and how the user changed it.
    - screenshot ingestion stores metadata and storage path/status only.
  </behavior>
  <action>Create numbered SQL migrations following existing Supabase style. Enable RLS on new user-owned tables. Use auth.uid() ownership policies. Keep money movement out of schema behavior; accounts and transactions are screenshot/manual records only.</action>
  <verify>
    <automated>powershell -NoProfile -Command "Select-String -Path 'supabase/migrations/07_behavior_events_and_questions.sql','supabase/migrations/08_generated_plans_and_screenshots.sql' -Pattern 'create table','enable row level security','behavior_events','plan_actions','screenshot' -Quiet"</automated>
  </verify>
  <done>Migrations define Phase 0 persistence contracts and RLS policies.</done>
</task>

<task type="auto">
  <name>Task 3: Register Phase 0 DB types</name>
  <files>src/lib/supabase.ts</files>
  <action>Add TypeScript types for the new DB rows in the central registry only. Follow existing naming and snake_case DB column conventions. Do not scatter DB types into hooks or screens.</action>
  <verify>
    <automated>npx tsc --project tsconfig.app.json --noEmit</automated>
  </verify>
  <done>Application code can import typed Phase 0 DB contracts from src/lib/supabase.ts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| client to Supabase | User actions and screenshots cross from device into storage/database. |
| household sharing | Joint Ops may expose limited behavior metadata to a partner. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-00-02-01 | Information Disclosure | screenshot_ingestions | mitigate | Store owner id, enable RLS, and keep parsing/manual confirmation separate from raw upload metadata. |
| T-00-02-02 | Tampering | behavior_events | mitigate | Use auth.uid() ownership policies and append-only event semantics unless a later wipe deletes records. |
| T-00-02-03 | Repudiation | wipe data | mitigate | Preserve a local UI success state but do not keep deleted user data as archive. |
</threat_model>

<verification>
Run `npx tsc --project tsconfig.app.json --noEmit` and inspect migrations before applying to remote Supabase.
</verification>

<success_criteria>
Phase 0 has a typed, RLS-protected data contract for behavior learning, generated plans, screenshots, wipe data, and kill switch follow-up work.
</success_criteria>

<output>
After completion, create `.planning/phases/00-level-0-bunker-reconstruction/00-02-SUMMARY.md`.
</output>
