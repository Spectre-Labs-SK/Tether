---
phase: 00-level-0-bunker-reconstruction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/FITNESS_PLAN.md
  - .planning/TETHER_ML_ARCHITECTURE.md
  - .planning/BUILD_PLAN.md
autonomous: true
requirements:
  - P0-SPEC-CONSOLIDATION
  - P0-NO-HARDCODED-WORKOUTS
must_haves:
  truths:
    - Phase 0 source docs say the first slice is Bunker Reconstruction, not a hardcoded workout app.
    - Fitness is documented as AI-generated/adaptive Joint Ops support.
    - ML architecture is documented as behavior logging and cautious inference first.
  artifacts:
    - .planning/FITNESS_PLAN.md
    - .planning/TETHER_ML_ARCHITECTURE.md
  key_links:
    - BUILD_PLAN points implementers to the filled source specs.
---

<objective>
Consolidate the Phase 0 planning source docs so every later implementation plan starts from the same product laws.

Purpose: Prevent agents from rebuilding the stale hardcoded fitness foundation.
Output: Filled source specs for Phase 0 fitness and intelligence architecture, plus a current build-plan reference.
</objective>

<execution_context>
@C:/Users/Windows 11 Pro/Desktop/Spectre_Labs/Feu_Follet/Tether_Safe/.Codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Windows 11 Pro/Desktop/Spectre_Labs/Feu_Follet/Tether_Safe/.Codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/BUILD_PLAN.md
@.planning/LEVEL_0_BUNKER_RECONSTRUCTION.md
@.planning/INTELLIGENCE_LAYER.md
@.planning/FITNESS_PLAN.md
@.planning/TETHER_ML_ARCHITECTURE.md
@.wolf/roadmap.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize Phase 0 fitness source spec</name>
  <files>.planning/FITNESS_PLAN.md</files>
  <action>Ensure the file defines fitness as AI-generated/adaptive Joint Ops and Ghost Ops support inside Bunker Reconstruction. State explicitly that Phase 0 must not build a hardcoded Iron/Road/Mat/Hub workout-plan foundation. Include the max-three-question rule and require skip, substitute, shuffle, defer, and correction actions as behavior signals.</action>
  <verify>
    <automated>powershell -NoProfile -Command "Select-String -Path '.planning/FITNESS_PLAN.md' -Pattern 'No hardcoded workout plans','Joint Ops','Ghost Ops','at most three','substitute','defer' -Quiet"</automated>
  </verify>
  <done>Fitness source spec is usable by a later executor without reinterpreting legacy hardcoded screens as the product direction.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize Phase 0 intelligence source spec</name>
  <files>.planning/TETHER_ML_ARCHITECTURE.md</files>
  <action>Ensure the file defines Phase 0 as data spine first: day-one behavior events, question sessions, generated plans, plan actions, screenshot ingestion stored before parsing, cautious inference, pgvector direction, and non-clinical warning language. Explicitly forbid direct bank access and automated money movement.</action>
  <verify>
    <automated>powershell -NoProfile -Command "Select-String -Path '.planning/TETHER_ML_ARCHITECTURE.md' -Pattern 'Behavior event logging','Screenshot-Only Finance Model','no bank-account access','pgvector','Never diagnose' -Quiet"</automated>
  </verify>
  <done>ML architecture source spec is ready to drive schema and implementation plans.</done>
</task>

</tasks>

<verification>
Run `npx tsc --project tsconfig.app.json --noEmit` and `npm run lint` after the full Phase 0 plan upload lands.
</verification>

<success_criteria>
The Phase 0 source docs make it impossible to confuse legacy hardcoded fitness implementation artifacts with the current AI-first Bunker direction.
</success_criteria>

<output>
After completion, create `.planning/phases/00-level-0-bunker-reconstruction/00-01-SUMMARY.md`.
</output>
