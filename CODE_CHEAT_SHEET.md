# EXECUTION ARCHIVE

## Agent Commands

gemini /plan "Audit the recent EntryGate refactor. Ensure useTetherState.ts logic remains consistent with the Valkyrie Gear registry. Flag any 'Bitch-Weight' logic that bypasses HR tracking.
alias sentinel-sync='git log -p -1 > .gsd/sentinel_latest.diff'